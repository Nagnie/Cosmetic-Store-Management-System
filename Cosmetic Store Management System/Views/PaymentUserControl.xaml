<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Cosmetic_Store_Management_System.Views.PaymentUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cosmetic_Store_Management_System.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local1="using:Cosmetic_Store_Management_System.Helpers"
    mc:Ignorable="d">
    <UserControl.Resources>
        <local1:IntToCurrencyConverter x:Key="IntToCurrencyConverter"/>
    </UserControl.Resources>


    <Grid Padding="12, 12, 12, 12"
          BorderBrush="#EEEEEE"
          BorderThickness="1"
          CornerRadius="12"
          Margin="0, 12, 0, 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Grid.Column="0" 
                   Text="Subtotal"
                   Margin="0, 0, 0, 10"
                   x:Uid="PaymentUserControl_Subtotal"/>
        <TextBlock Grid.Row="0" 
                   Grid.Column="1" 
                   Text="{x:Bind ViewModel.SubTotal, Mode=OneWay, 
                        Converter={StaticResource IntToCurrencyConverter}}" 
                   HorizontalAlignment="Right"
                   Margin="0, 0, 0, 10"/>

        <TextBlock Grid.Row="1" 
                   Grid.Column="0" 
                   Text="Discount"
                   Margin="0, 0, 0, 10"
                   x:Uid="PaymentUserControl_Discount"/>
        <TextBlock Grid.Row="1" 
                   Grid.Column="1" 
                   Text="{x:Bind ViewModel.Discount, Mode=OneWay, 
                        Converter={StaticResource IntToCurrencyConverter}}" 
                   HorizontalAlignment="Right"
                   Margin="0, 0, 0, 10"/>

        <TextBlock Grid.Row="2" 
                   Grid.Column="0" 
                   Text="Sale Tax"
                   Margin="0, 0, 0, 14"
                   x:Uid="PaymentUserControl_SaleTax"/>
        <TextBlock Grid.Row="2" 
                   Grid.Column="1" 
                   Text="{x:Bind ViewModel.SaleTax, Mode=OneWay, 
                        Converter={StaticResource IntToCurrencyConverter}}" 
                   HorizontalAlignment="Right"
                   Margin="0, 0, 0, 14"/>

        <TextBlock Grid.Row="3" 
                   Grid.Column="0" 
                   Text="Total"
                   FontSize="16"
                   FontWeight="Bold"
                   x:Uid="PaymentUserControl_Total"/>
        <TextBlock Grid.Row="3" 
                   Grid.Column="1" 
                   Text="{x:Bind ViewModel.Total, Mode=OneWay, 
                        Converter={StaticResource IntToCurrencyConverter}}" 
                   FontSize="16"
                   FontWeight="Bold"
                   HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
