<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Cosmetic_Store_Management_System.Views.EditCategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cosmetic_Store_Management_System.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ID"
                   Grid.Row="0"
                   Grid.Column="0"
                   Margin="0, 0, 0, 12"/>
        <TextBox Text="{x:Bind ViewModel.Category.ID, Mode=TwoWay}"
                 IsEnabled="False"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0, 0, 0, 12"/>

        <TextBlock Text="Name"
                   Grid.Row="1"
                   Grid.Column="0"
                   Margin="0, 0, 0, 12"
                   x:Uid="EditCategoryPage_Name"/>
        <TextBox Text="{x:Bind ViewModel.Category.Name, Mode=TwoWay}"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="0, 0, 0, 12"/>

        <TextBlock Text="Product Count"
                   Grid.Column="0"
                   Grid.Row="2"
                   x:Uid="EditCategoryPage_ProductCount"/>
        <TextBox Text="{x:Bind ViewModel.Category.productCount, Mode=TwoWay}"
                 Grid.Column="1"
                 Grid.Row="2"
                 ScrollViewer.HorizontalScrollMode="Enabled"
                 TextWrapping="Wrap"
                 Height="120"/>

        <StackPanel Grid.Column="1"
                    Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0, 16, 0, 0">
            <Button Content="Save"
                    Width="80"
                    Margin="0, 0, 12, 0"
                    Click="saveButton_Click"
                    x:Uid="EditCategoryPage_SaveButton"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="cancelButton_Click"
                    x:Uid="EditCategoryPage_CancelButton"/>
        </StackPanel>
    </Grid>
</Page>
